jQuery(document).ready(function() {
	GpsGate.Server.Hackathon.ReportPosition("t5test", "foo", "yooooo", "t5test", 0, 0);
/*	navigator.geolocation.getCurrentPosition(function (position) {
		console.log(position);
  });
*/});
